package com.pulse1.config;

import org.springframework.context.annotation.Configuration;

/*
  NOTE: Security is intentionally permissive in this starter project so you can run all endpoints during development/demo.
  For production, add JWT authentication and restrict endpoints appropriately.
*/
@Configuration
public class SecurityConfig {
    // placeholder - no security enforced in this starter
}